# Tests

Target home for top-level test organization.

Current status: scaffold. The current executable app tests still live under `mofacts/` and must remain covered by `npm run typecheck` and existing test commands until root test wiring changes.

Belongs here:

- App-level tests.
- Learning component contract tests.
- Integration tests.
- E2E tests.
- Shared fixtures.

Do not move tests here until the test runner is updated to discover and execute them.
