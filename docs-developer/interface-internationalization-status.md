# Interface Internationalization Status

This record tracks the implementation state for `interface-internationalization-plan.md`.

## Locale Status

All ten initial target locales have checked-in AI-draft platform strings. Human review is still required before a locale is considered production-reviewed.

| Locale | Language | Direction | Primary platform TTS code | Platform string status | Production enablement |
| --- | --- | --- | --- | --- | --- |
| `en` | English | LTR | `en-US` | Draft complete | enabled baseline |
| `zh-Hans` | Mandarin Chinese | LTR | `cmn-CN` | AI draft complete | review required |
| `hi` | Hindi | LTR | `hi-IN` | AI draft complete | review required |
| `es` | Spanish | LTR | `es-ES` | AI draft complete | review required |
| `ar` | Standard Arabic | RTL | `ar-XA` | AI draft complete | review required |
| `fr` | French | LTR | `fr-FR` | AI draft complete | review required |
| `bn` | Bengali | LTR | `bn-IN` | AI draft complete | review required |
| `pt` | Portuguese | LTR | `pt-BR` | AI draft complete | review required |
| `id` | Indonesian | LTR | `id-ID` | AI draft complete | review required |
| `ur` | Urdu | RTL | `ur-IN` | AI draft complete | review required |

## Language Ownership Map

| Concept | Current implementation owner | Runtime meaning | Notes |
| --- | --- | --- | --- |
| UI locale | `mofacts/common/lib/interfaceLocales.ts`, `mofacts/common/lib/interfaceLocaleSelection.ts`, `mofacts/client/lib/interfaceLocaleState.ts` | Platform chrome, formatting, document direction, platform-owned prompt TTS selection, and locale review status. | User profile preference persists as `profile.uiLocale`; Session override is available for local/reactive switching. English is production-enabled; non-English target locales are draft pending qualified review. |
| Platform strings | `mofacts/client/lib/interfaceI18nResources.ts` and `mofacts/client/lib/interfaceI18n.ts` | Versioned MoFaCTS UI strings. | Missing locale/key/interpolation values fail clearly. |
| Content language | TDF `tdfs.tutor.setspec.contentLanguage` | Authored instructional content language. | Used for learner text input `lang`/`dir`; does not translate authored content. |
| Recommended UI locales | TDF `tdfs.tutor.setspec.recommendedUiLocales` | Author/package recommendation for platform chrome. | Metadata only in the current slice. |
| Content translation status | TDF `tdfs.tutor.setspec.translationStatus` | Author-declared review state for a content-language variant. | Does not cause automatic translation. |
| Learner response normalization | `learning-components/content/response-normalization/learnerResponseNormalization.ts`, `mofacts/client/views/experiment/learnerResponseAssessment.ts`, and `tutor.deliverySettings` | Unicode normalization, case policy, and accent policy for typed answers. | `caseSensitive` and `accentSensitive` are explicit delivery settings that can be authored at lesson/unit scope and are passed into runtime answer evaluation. |
| Speech recognition language | TDF `speechRecognitionLanguage`, UI-locale primary SR mapping, and speech-recognition services | Provider language code for microphone transcription. | Remains opt-in and separate from content language; explicit TDF metadata wins, and SR-enabled lessons without TDF language use the active UI locale's primary SR code. |
| Authored content TTS language | TDF `textToSpeechLanguage` and authored-content branch in `ttsService.ts` | Spoken authored prompts/feedback when audio is enabled. | Preserves existing lesson-owned TTS behavior. |
| Platform prompt TTS language | `resolvePlatformPromptTtsLanguage` and platform-prompt branch in `ttsService.ts` | Spoken platform-owned prompts in selected UI locale. | Explicit mapping only; unsupported or missing voice conditions report errors instead of substituting English. |

## Implemented Surfaces

- App loading/status chrome uses locale resources for initial loading and readiness messages.
- App shell menu/close labels, practice return control, home sidebar navigation, account menu, compact practice menu, role label, and home guided-tour text use locale resources.
- Profile debug toggle chrome uses locale resources for the debug-menu heading and probability-parameter display control.
- Learner dashboard uses locale resources for resume card metrics, search controls, lesson summary, loading and empty states, section headings, table/card headers, action buttons, details labels, lesson feature tooltips, settings panel status/actions, and dashboard launch/error messages.
- Course listing shell uses locale resources for search/sort controls, loading and empty states, table/card headers, status labels, action labels, due/release prefixes, and time-unit labels.
- Admin Controls uses locale resources for server status/storage labels, stimulus display cache maintenance copy/actions, server logging controls, browser console logging controls, and local admin status/error messages.
- User Admin uses locale resources for page/section headings, list controls, pagination labels, table headers, role/action controls, delete-user confirmation copy, import-user controls, the access-denied message, local action-status messages for API-key administration, API-key provider labels, news-email preparation, usage-cache refresh, role changes, user deletion, and batch user import, plus usage-cache table display strings and API-key metadata placeholder/status strings.
- Audio Settings uses locale resources for TTS/SR headings, toggles, descriptions, volume/speed/voice controls, voice labels, speech-detection threshold guidance, sound-level reference table, Speech API key setup copy, save/delete actions, and local save/delete status messages.
- Admin Backups uses locale resources for page headings, same-server backup warning copy, backup configuration labels, create/history/action buttons, empty state, selected-manifest heading, restore/delete confirmation panel copy, enabled/disabled/not-configured labels, admin-access-required copy, and local backup/create/verify/manifest/download/delete/restore status messages.
- Admin Tests uses locale resources for deployment-readiness headings, description, action button, running/result status messages, readiness table headers, and pass/fail status labels.
- Content Manager uses locale resources for the page title, quota banner, uploaded-content table headers, public/private visibility switch labels, pending upload badges/progress/error-label text in the table, stimulus/media manager controls, media file table headers/tooltips, access/ownership controls and local status messages, condition/stim selector labels and missing-file markers, package action tooltips, upload-package heading, Anki/Canvas import section headings/descriptions/actions, copy/delete/download/media-upload confirmations and status messages, package upload/overwrite/processing confirmations and status messages, and delete-all admin warning/action.
- Course Management uses locale resources for course-edit headings, loading/empty states, course visibility/date/timezone/section controls, timezone labels, save/delete actions, and local validation/save/delete confirmation and status messages.
- Course/class selection uses locale resources for join-course headings, enrollment/current-course status, instructor/course selectors, save/back actions, loading text, and local validation/save status messages.
- Course Assignments uses locale resources for assignment-editor headings, course selector, lesson search, selected-assignment status, loading/empty states, order/action tooltips, required/release/due controls, reset/save actions, and local assignment validation messages.
- AI Content Creator uses locale resources for authoring chrome, OpenRouter setup warning/action, source input prompt and accessibility label, character count, mode selector labels/helper text, manual-create action, create button states, local validation/status messages, AutoTutor creation progress, and generated-name conflict prompt.
- TDF Editor uses locale resources for edit-page headings, condition context, loading/not-found states, unsaved-change badge, description verbosity controls, cancel/save actions, schema/custom-validation messages, save feedback, schema-load errors, and editor-library load errors.
- Content/Stimulus Editor uses locale resources for edit-page headings, stimulus-file context, loading/not-found/initializing states, shared editor chrome, incorrect-response generation/removal controls and confirmations, cluster navigation/window labels, schema/custom-validation messages, save feedback, schema/editor-load errors, and media preview/upload feedback.
- SPARC authoring route/header/palette chrome uses locale resources for loading/not-found route states, content-manager return action, visual-editor title, advanced-editor toggle, cancel/save actions, saving state, target-page label, advanced tab labels, editor tablist accessibility label, palette heading, and palette accessibility label.
- SPARC rich-text toolbar and reusable rule-expression editor use locale resources for toolbar accessibility/group labels, node-hierarchy toggle, formatting/list/alignment/link/media/table/history controls, image alt placeholder text, reusable expression field labels, argument headings, add-argument action, and remove action.
- SPARC advanced production-rule editor uses locale resources for panel headings, add/move/delete controls, rule summaries, empty states, common field labels, condition/test/effect section labels, slot ARIA labels, expression labels, target/node/state labels, and production-rule guidance while preserving rule IDs, type values, cluster labels, JSON, and authored effect/template text as data.
- SPARC scoped/selected-node side panels use locale resources for scoped-rule template controls, add-rule-for-selection action, scoped-rule empty states and JSON labels, selected-node actions and media-editing labels, local-host embed warning, dropdown/button/panel-selector field labels, cluster-attachment heading, and generated cluster-checkbox ARIA labels while preserving node IDs, rule IDs, enum values, cluster labels, HTML, JSON, and authored node content as data.
- SPARC visual surface and rich-text runtime messages use locale resources for drop-surface and node-hierarchy accessibility labels, drag/drop placement status, rich-text placeholder, HTTPS URL validation errors, selected-node delete confirmation, no-active-display error, empty-selection prompt, invalid-fraction display message, SPARC AutoTutor conversation accessibility label, and saved status.
- Manual Content Creator shell/lifecycle uses locale resources for route header, draft save/update/delete actions, wizard step labels/headings, top-level navigation buttons, draft-load/save/delete messages, delete-draft confirmation, status summary, package-ready/uploaded states, final package action buttons, upload/overwrite confirmations and progress/status messages.
- Manual Content Creator embedded draft workspace uses locale resources for editor headings, save/continue label, lesson selector label, lesson-count labels, TDF/content tabs, reset/back actions, manifest card/media/skipped summary terms, and draft-editor validation/init error messages.
- Manual Content Creator dynamic content-draft editor uses locale resources for JSONEditor edit-properties/JSON button chrome, cluster-window navigation controls, cluster range status, window-size label, performance helper text, no-cluster empty state, and editor-control initialization failure.
- Manual Content Creator summary/finalization copy uses locale resources for step intro descriptions, finalize/package status copy, included-lessons label, summary labels, summary option values for structure/prompt/response/top-bar, visibility summary value, and pending/off link summary states.
- Manual Content Creator form option values use locale resources for structure modes, visibility radio labels, prompt/response types, button-order choices, text-to-speech modes, and top-bar modes.
- Manual Content Creator starter-content section uses locale resources for starting-mode label/options, starter-mode helper text, paste-table label/placeholder/expected-column text, row-count summary, add-row action, answer/choice/action table headers, and associated draft strings.
- Manual Content Creator basics/card/audio field copy uses locale resources for lesson-name, instruction, visibility, experiment-link, prompt/response/card/shuffle/button-order, speech-recognition, text-to-speech, top-bar, timing, and tag labels/helpers/placeholders; the `en-US` speech-language placeholder remains a literal provider code example.
- Manual Content Creator visible platform copy is localized for the wizard shell, save/delete lifecycle, mode controls, summary/finalization panels, embedded draft workspace, starter-content table, and basics/card/audio/display field controls. Authored lesson fields remain author-entered content.
- Manual Content Creator exposes optional author-controlled language metadata fields for `contentLanguage`, `recommendedUiLocales`, and `translationStatus`, validates BCP 47-style tags, and emits metadata into generated TDF `setspec` without translating authored prompts or answers.
- Manual Content Creator emits `textToSpeechLanguage` only when text-to-speech is enabled and an explicit speech language is provided; speech recognition can be enabled without TDF `speechRecognitionLanguage`, and emits that field only when the author supplies an explicit provider-language override.
- Manual Content Creator exposes typed-answer matching controls for case sensitivity and accent sensitivity, localizes those controls across the ten target UI locales, and emits the selected policy into generated tutor/unit `deliverySettings`.
- Manual Content Creator Bengali and Urdu draft resources have been polished for obvious English label leftovers in the basics/card/audio cluster, and Hindi speech/TTS labels now use Hindi draft wording instead of mixed English labels.
- Anki import wizard upload-step chrome uses locale resources for the wizard title/description, stepper labels, inline cancel action, select-deck heading, file-picker label/help text, analysis progress labels, upload-step error heading, cancel button, and analyze-deck action.
- Anki import wizard setup step uses locale resources for deck metadata labels, importable-note count, available-fields labels, field type labels, samples/no-samples labels, TDF configuration headings/actions, field selectors, note-range labels/help, selected-note summaries, configuration-valid status, add-configuration help, setup summary lines, back/open-draft actions, packaging-error heading, and remove-config confirmations.
- Anki import wizard package/upload step uses locale resources for package-generation headings/status, generated-file summaries, skipped-card warnings, download/upload actions, upload progress hints, overwrite confirmations, package-processing errors, close-wizard confirmation, and setup validation errors.
- Canvas IMSCC import wizard uses locale resources for upload/analyze chrome, setup output-mode controls, metadata/table labels, quiz selection summaries, joined/separate conversion settings, validation messages, draft-preparation status, package-generation headings/status, generated-file summaries, manifest count labels, download/upload actions, upload progress hints, package-processing errors, and close-wizard action.
- Reporting/Data Download uses locale resources for data-download headings, download actions, table headers, loading/empty states, link tooltips, condition-count display, instructor-reporting headings, class/module selectors, deadline filters, performance table headers, due-date grouping rows, exception actions, totals/empty rows, and local download/exception status messages.
- Mechanical Turk admin workflow uses locale resources for AWS profile headings/placeholders/actions, saved-profile summaries, experiment-log selectors/table headers/empty states, assignment lookup controls, Turk message/removal controls, modal titles/loading text, result/detail messages, pay/bonus status messages, and local timestamp formatting while preserving AWS, TDF, MongoDB, AssignmentID, WorkerID, and Mechanical Turk identifiers as product/data terms.
- Theme Settings editor library chrome uses locale resources for the page title, inline confirmation cancel button, theme-library heading, active-server-theme label, active/export/reset/import actions, library empty state, theme-name label, active/activate state labels, system/custom origin labels, delete-theme confirmation copy, Branding controls/status messages, Custom Help Page controls/status messages, core section headers, Main Layout labels/help text, Buttons/Interactive labels/help text, Menus/Secondary labels, Alerts/Feedback labels/help text, Card Surfaces/Overlays labels/help text, Border Radius labels, Animation labels, contrast labels, and compact default/range/used-for explanatory text.
- Theme Generation Wizard uses locale resources for generator headings, palette controls, polarity/density/contrast controls, palette expansion options, palette statistics, generated-theme preview labels, diagnostics headings, action buttons, default generated names, default palette-slot labels, and local palette/theme validation and status messages.
- Login, signup, password-reset, and email-verification templates use locale resources for static platform-owned account text, email-verification lifecycle status messages, resend-verification validation/status messages, and verification error fallbacks while preserving server-provided error reasons when supplied.
- Help page platform chrome uses locale resources for the student-help heading, administrator-contact prompt, and help-content load-failure message while preserving custom or wiki Markdown help content as authored documentation.
- Profile page exposes an interface-language selector backed by `profile.uiLocale`, and profile chrome uses locale resources for private/public profile labels, avatar controls, AI-provider controls, save/test/delete actions, and local avatar/profile status messages.
- Public repository documentation now includes `docs/interface-internationalization.md`, which summarizes the supported UI locale set, authored-content language separation, input/audio behavior, review requirement, and no-silent-substitution failure policy.
- Developer documentation now includes `docs-developer/interface-i18n-translation-review-checklist.md`, which defines reviewer evidence, string-review checks, layout/runtime checks, signoff template, and production-enable gates for AI-draft locale resources.
- Target UI locale definitions track review status in code with English enabled and the other nine target locales marked draft pending qualified human review.
- Locale-aware formatting and collation are wired into Admin Backups timestamps, Theme custom-help upload timestamps, Canvas IMSCC due-date display, Course listing date display, Mechanical Turk workflow timestamps, learner-dashboard fallback last-practice dates, and learner-dashboard unused-lesson name sorting.
- Learner-dashboard lesson feature icons are scoped to lesson/learner-config audio settings, so global user audio preferences no longer make lessons with disabled TTS/SR metadata advertise TTS or speech-recognition support.
- Standard flashcard learner text input and SPARC text/fraction inputs receive `lang` and `dir` from declared `contentLanguage`.
- Standard flashcard and SPARC text inputs ignore Enter while an IME composition is active, preventing premature submission for CJK and other composition-based input.
- Standard flashcard typed-answer input now preserves the actual DOM input value through the Svelte response component chain before submit, so accented and non-ASCII learner answers are not clobbered by parent rerenders before answer evaluation/history logging.
- Delivery settings expose author-controlled `accentSensitive` answer matching next to the existing `caseSensitive` option, and the Svelte card runtime passes both options into answer evaluation.
- Answer assessment treats pipe-delimited literal alternatives as script-agnostic authored alternatives, so non-Latin answers such as Chinese alternatives are no longer gated by an ASCII-only pattern.
- Speech recognition availability, startup warmup, Audio Settings SR warmup, and runtime transcription no longer block SR-enabled lessons that omit `speechRecognitionLanguage`; explicit TDF language still wins, and missing TDF language resolves from the active UI locale's primary SR code.
- Primary SR codes are tracked separately from primary TTS codes, including `zh-Hans` -> `cmn-Hans-CN` and `ar` -> `ar-SA` for Google Speech-to-Text.
- `npm run test:ci` was run locally with explicit user authorization and `CI=1`; the current Meteor integration run executed server tests and reported 437 passing, 12 pending, and exit code 0.
- `npm run generate:schemas` from `mofacts/` passed after adding `cmn-Hans-CN` to the shared Google STT language registry; the generated TDF schema now includes the STT enum addition and the previously implemented `accentSensitive` delivery-setting schema output.
- `npm run typecheck` from `mofacts/` passed after correcting the UI-locale primary SR mapping.
- `npm run lint` from `mofacts/` passed after correcting the UI-locale primary SR mapping.
- TTS warmup, authored-content TTS resolution, and the server Google TTS method require explicit TTS language evidence from `textToSpeechLanguage` or an explicit voice-locale id; missing TTS language now fails clearly instead of substituting `en-US`.
- TTS playback returns a soft audio-unavailable result with `textAvailable: true` when language or voice resolution fails, preserving visual lesson text while reporting the audio problem clearly.
- Learner performance stat bars, learning-progress panel/chart, and AutoTutor/SPARC AutoTutor runtime progress chrome use locale resources for session-stats accessibility labels, time/minute/correct labels, progress panel labels, chart labels, chart accessibility summaries, default empty-state text, AutoTutor progress labels, AutoTutor placeholders, AutoTutor completion/status messages, instruction back-button accessibility text, replay-audio accessibility/title text, and Svelte timeout-countdown messages.
- Learner runtime fallback controls use locale resources for platform-owned Continue labels, force-correct input placeholder, video loading/autoplay-blocked messages, H5P iframe accessibility title, invalid H5P display fallback text, feedback outcome labels, feedback sentence shells, correct-answer image alt text, and screen-reader feedback announcements while preserving authored `continueButtonText`, authored outcome-label overrides, and authored answer text when provided by lesson settings/content.

## Current Verification

- `npm run generate:schemas` from `mofacts/` passed after adding TDF language metadata fields.
- `npm run typecheck` from `mofacts/` passed after the Content Manager package-upload/overwrite/processing localization slice.
- `npm run lint` from `mofacts/` passed after the Content Manager package-upload/overwrite/processing localization slice.
- `npm run typecheck` from `mofacts/` passed after the User Admin template-chrome localization slice.
- `npm run lint` from `mofacts/` passed after the User Admin template-chrome localization slice.
- `npm run typecheck` from `mofacts/` passed after the User Admin action-status localization slice.
- `npm run lint` from `mofacts/` passed after the User Admin action-status localization slice.
- `npm run typecheck` from `mofacts/` passed after the User Admin usage-cache/API-key metadata display localization slice.
- `npm run lint` from `mofacts/` passed after the User Admin usage-cache/API-key metadata display localization slice.
- `npm run typecheck` from `mofacts/` passed after the Audio Settings localization slice.
- `npm run lint` from `mofacts/` passed after the Audio Settings localization slice.
- `npm run typecheck` from `mofacts/` passed after the Admin Backups template-chrome localization slice.
- `npm run lint` from `mofacts/` passed after the Admin Backups template-chrome localization slice.
- `npm run typecheck` from `mofacts/` passed after the Admin Backups operation-message localization slice.
- `npm run lint` from `mofacts/` passed after the Admin Backups operation-message localization slice.
- `npm run typecheck` from `mofacts/` passed after the Reporting/Data Download localization slice.
- `npm run lint` from `mofacts/` passed after the Reporting/Data Download localization slice.
- `npm run typecheck` from `mofacts/` passed after the Admin Tests localization slice.
- `npm run lint` from `mofacts/` passed after the Admin Tests localization slice.
- `npm run typecheck` from `mofacts/` passed after the learner runtime fallback-control localization slice.
- `npm run lint` from `mofacts/` passed after the learner runtime fallback-control localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Generation Wizard localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Generation Wizard localization slice.
- `npm run typecheck` from `mofacts/` passed after the Course Management localization slice.
- `npm run lint` from `mofacts/` passed after the Course Management localization slice.
- `npm run typecheck` from `mofacts/` passed after the Course Assignments localization slice.
- `npm run lint` from `mofacts/` passed after the Course Assignments localization slice.
- `npm run typecheck` from `mofacts/` passed after the AI Content Creator localization slice.
- `npm run lint` from `mofacts/` passed after the AI Content Creator localization slice.
- `npm run typecheck` from `mofacts/` passed after the Manual Content Creator shell/lifecycle localization slice.
- `npm run lint` from `mofacts/` passed after the Manual Content Creator shell/lifecycle localization slice.
- `npm run typecheck` from `mofacts/` passed after the Manual Content Creator embedded draft-workspace localization slice.
- `npm run lint` from `mofacts/` passed after the Manual Content Creator embedded draft-workspace localization slice.
- `npm run typecheck` from `mofacts/` passed after the Manual Content Creator summary/finalization localization slice.
- `npm run lint` from `mofacts/` passed after the Manual Content Creator summary/finalization localization slice.
- `npm run typecheck` from `mofacts/` passed after the Manual Content Creator form-option localization slice.
- `npm run lint` from `mofacts/` passed after the Manual Content Creator form-option localization slice.
- `npm run typecheck` from `mofacts/` passed after the Manual Content Creator starter-content localization slice.
- `npm run lint` from `mofacts/` passed after the Manual Content Creator starter-content localization slice.
- `npm run typecheck` from `mofacts/` passed after the Manual Content Creator basics/card/audio field-copy localization slice.
- `npm run lint` from `mofacts/` passed after the Manual Content Creator basics/card/audio field-copy localization slice.
- `npm run typecheck` from `mofacts/` passed after the Anki import wizard upload-step localization slice.
- `npm run lint` from `mofacts/` passed after the Anki import wizard upload-step localization slice.
- `npm run typecheck` from `mofacts/` passed after the Anki import wizard setup-step localization slice.
- `npm run lint` from `mofacts/` passed after the Anki import wizard setup-step localization slice.
- `npm run typecheck` from `mofacts/` passed after the Anki import wizard package/upload-step and validation-error localization slice.
- `npm run lint` from `mofacts/` passed after the Anki import wizard package/upload-step and validation-error localization slice.
- `npm run typecheck` from `mofacts/` passed after the TDF Editor localization slice.
- `npm run lint` from `mofacts/` passed after the TDF Editor localization slice.
- `npm run typecheck` from `mofacts/` passed after the Content/Stimulus Editor localization slice.
- `npm run lint` from `mofacts/` passed after the Content/Stimulus Editor localization slice.
- `npm run typecheck` from `mofacts/` passed after the Canvas IMSCC import wizard upload/analyze-step localization slice.
- `npm run lint` from `mofacts/` passed after the Canvas IMSCC import wizard upload/analyze-step localization slice.
- `npm run typecheck` from `mofacts/` passed after the Canvas IMSCC import wizard setup/package/upload localization slice.
- `npm run lint` from `mofacts/` passed after the Canvas IMSCC import wizard setup/package/upload localization slice.
- `npm run typecheck` from `mofacts/` passed after the SPARC authoring header/palette localization slice.
- `npm run lint` from `mofacts/` passed after the SPARC authoring header/palette localization slice.
- `npm run typecheck` from `mofacts/` passed after the SPARC authoring route-wrapper localization slice.
- `npm run lint` from `mofacts/` passed after the SPARC authoring route-wrapper localization slice.
- `npm run typecheck` from `mofacts/` passed after the SPARC rich-text toolbar and reusable expression-editor localization slice.
- `npm run lint` from `mofacts/` passed after the SPARC rich-text toolbar and reusable expression-editor localization slice.
- `npm run typecheck` from `mofacts/` passed after the SPARC advanced production-rule editor localization slice.
- `npm run lint` from `mofacts/` passed after the SPARC advanced production-rule editor localization slice.
- `npm run typecheck` from `mofacts/` passed after the SPARC scoped/selected-node side-panel localization slice.
- `npm run lint` from `mofacts/` passed after the SPARC scoped/selected-node side-panel localization slice.
- `npm run typecheck` from `mofacts/` passed after the SPARC visual-surface and rich-text runtime-message localization slice.
- `npm run lint` from `mofacts/` passed after the SPARC visual-surface and rich-text runtime-message localization slice.
- `npm run typecheck` from `mofacts/` passed after the Manual Content Creator dynamic content-draft editor localization slice.
- `npm run lint` from `mofacts/` passed after the Manual Content Creator dynamic content-draft editor localization slice.
- `npm run typecheck` from `mofacts/` passed after the email-verification account-flow localization slice.
- `npm run lint` from `mofacts/` passed after the email-verification account-flow localization slice.
- `npm run typecheck` from `mofacts/` passed after the course/class selection localization slice.
- `npm run lint` from `mofacts/` passed after the course/class selection localization slice.
- `npm run typecheck` from `mofacts/` passed after the profile debug-toggle localization slice.
- `npm run lint` from `mofacts/` passed after the profile debug-toggle localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings library-chrome localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings library-chrome localization slice.
- `npm run typecheck` from `mofacts/` passed after the learner performance-stat localization slice.
- `npm run lint` from `mofacts/` passed after the learner performance-stat localization slice.
- `npm run typecheck` from `mofacts/` passed after the Help page platform-chrome localization slice.
- `npm run lint` from `mofacts/` passed after the Help page platform-chrome localization slice.
- `npm run typecheck` from `mofacts/` passed after the User Admin API-key provider-label localization slice.
- `npm run lint` from `mofacts/` passed after the User Admin API-key provider-label localization slice.
- `npm run typecheck` from `mofacts/` passed after the Content Upload error-label localization slice.
- `npm run lint` from `mofacts/` passed after the Content Upload error-label localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings Custom Help Page localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings Custom Help Page localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings Branding localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings Branding localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings section-header/default-help localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings section-header/default-help localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings Main Layout label/help localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings Main Layout label/help localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings Buttons/Interactive label/help localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings Buttons/Interactive label/help localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings Menus/Secondary label localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings Menus/Secondary label localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings Alerts/Feedback label/help localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings Alerts/Feedback label/help localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings Card Surfaces/Overlays label/help localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings Card Surfaces/Overlays label/help localization slice.
- `npm run typecheck` from `mofacts/` passed after the Theme Settings Border Radius/Animation label localization slice.
- `npm run lint` from `mofacts/` passed after the Theme Settings Border Radius/Animation label localization slice.
- `npm run typecheck` from `mofacts/` passed after the Content Manager condition/stim selector localization cleanup.
- `npm run lint` from `mofacts/` passed after the Content Manager condition/stim selector localization cleanup.
- `npm run typecheck` from `mofacts/` passed after the learner learning-progress panel/chart localization cleanup.
- `npm run lint` from `mofacts/` passed after the learner learning-progress panel/chart localization cleanup.
- `npm run typecheck` from `mofacts/` passed after the AutoTutor/SPARC runtime progress localization cleanup.
- `npm run lint` from `mofacts/` passed after the AutoTutor/SPARC runtime progress localization cleanup.
- `npm run typecheck` from `mofacts/` passed after the SPARC/learner-runtime leftover label localization cleanup.
- `npm run lint` from `mofacts/` passed after the SPARC/learner-runtime leftover label localization cleanup.
- `npm run typecheck` from `mofacts/` passed after the runtime accessibility/placeholder label localization cleanup.
- `npm run lint` from `mofacts/` passed after the runtime accessibility/placeholder label localization cleanup.
- `npm run typecheck` from `mofacts/` passed after the Mechanical Turk admin workflow localization slice.
- `npm run lint` from `mofacts/` passed after the Mechanical Turk admin workflow localization slice.
- `npm run typecheck` from `mofacts/` passed after adding the public interface-internationalization documentation.
- `npm run lint` from `mofacts/` passed after adding the public interface-internationalization documentation.
- `npm run typecheck` from `mofacts/` passed after moving Mechanical Turk workflow timestamps to active UI-locale formatting.
- `npm run lint` from `mofacts/` passed after moving Mechanical Turk workflow timestamps to active UI-locale formatting.
- `npm run typecheck` from `mofacts/` passed after moving Admin Backups, Theme, Canvas IMSCC, Course listing, and learner-dashboard formatting/collation paths to the active UI locale.
- `npm run lint` from `mofacts/` passed after moving Admin Backups, Theme, Canvas IMSCC, Course listing, and learner-dashboard formatting/collation paths to the active UI locale.
- `npm run typecheck` from `mofacts/` passed after adding the IME composition guard for standard flashcard and SPARC text-input Enter submission.
- `npm run lint` from `mofacts/` passed after adding the IME composition guard for standard flashcard and SPARC text-input Enter submission.
- `npm run typecheck` from `mofacts/` passed after requiring explicit speech-recognition language for SR-enabled availability, warmup, and transcription paths.
- `npm run lint` from `mofacts/` passed after requiring explicit speech-recognition language for SR-enabled availability, warmup, and transcription paths.
- `npm run typecheck` from `mofacts/` passed after adding locale review/production-enable status to the target-locale source of truth.
- `npm run lint` from `mofacts/` passed after adding locale review/production-enable status to the target-locale source of truth.
- `npm run generate:schemas` from `mofacts/` passed after adding `accentSensitive` delivery-setting schema support.
- `npm run typecheck` from `mofacts/` passed after adding `accentSensitive` delivery-setting runtime support.
- `npm run lint` from `mofacts/` passed after adding `accentSensitive` delivery-setting runtime support.
- `npm run typecheck` from `mofacts/` passed after strengthening `interfaceI18n` tests for missing target-locale resources and missing required translations.
- `npm run lint` from `mofacts/` passed after strengthening `interfaceI18n` tests for missing target-locale resources and missing required translations.
- `mofacts/common/lib/interfaceFormatting.test.ts` now asserts that all ten target UI locales format numbers, percents, and dates through the shared interface formatter without resolving non-English targets to English.
- `npm run typecheck` from `mofacts/` passed after adding all-ten-locale interface-formatting coverage.
- `npm run lint` from `mofacts/` passed after adding all-ten-locale interface-formatting coverage.
- `mofacts/common/lib/contentLanguageAttributes.test.ts` now covers non-English authored-content input attributes independently from UI locale selection.
- `mofacts/client/views/experiment/svelte/services/ttsService.test.ts` now covers mixed-language TTS separation: authored-content TTS follows `textToSpeechLanguage`, while platform-prompt TTS follows UI locale.
- `npm run typecheck` from `mofacts/` passed after adding mixed-language input/TTS boundary coverage.
- `npm run lint` from `mofacts/` passed after adding mixed-language input/TTS boundary coverage.
- `mofacts/client/lib/manualDraftBuilder.test.ts` now covers conditional manual-authoring emission of `textToSpeechLanguage`, optional emission of `speechRecognitionLanguage`, and SR-enabled drafts that omit TDF SR language metadata.
- `mofacts/client/lib/manualContentCreatorValidation.test.ts` now covers explicit speech-language validation for enabled TTS while allowing speech recognition without TDF SR language metadata.
- `npm run typecheck` from `mofacts/` passed after making Manual Content Creator audio-language metadata conditional and explicit, including the UI-locale SR default correction.
- `npm run lint` from `mofacts/` passed after making Manual Content Creator audio-language metadata conditional and explicit, including the UI-locale SR default correction.
- `mofacts/client/lib/audioLanguage.test.ts` now covers explicit TTS-language resolution from `textToSpeechLanguage` or voice-locale ids and verifies missing language/voice fails instead of substituting English.
- `mofacts/client/lib/audioStartup.test.ts` now requires explicit TTS language evidence before launch-time TTS warmup is planned.
- Targeted source search found no remaining implementation-time `textToSpeechLanguage || 'en-US'`, `DEFAULT_SPEECH_RECOGNITION_LANGUAGE`, server TTS `languageCode = 'en-US'`, `return 'en-US'` TTS/SR language substitution paths, or old SR `missing_language` availability gate outside tests. Missing TDF SR language now resolves through the active UI locale's explicit primary SR map.
- `npm run typecheck` from `mofacts/` passed after removing permissive TTS/SR English language substitution paths.
- `npm run lint` from `mofacts/` passed after removing permissive TTS/SR English language substitution paths.
- `mofacts/client/views/experiment/learnerResponseAssessment.test.ts` now covers composed/decomposed accents, Chinese pipe-delimited alternatives, Bengali, Arabic, Urdu, and Portuguese accent-policy behavior through the production answer-assessment API.
- `mofacts/common/lib/learnerResponseNormalization.test.ts` now covers Devanagari composed/decomposed normalization.
- `mofacts/common/lib/contentLanguageAttributes.test.ts`, `mofacts/common/lib/learnerResponseNormalization.test.ts`, `mofacts/client/views/experiment/learnerResponseAssessment.test.ts`, and `mofacts/client/views/experiment/svelte/services/flashcardTextInputController.test.ts` now include all-ten-target-language table coverage for authored-content `lang`/`dir`, Unicode learner-response normalization, production answer evaluation, and text-input state preservation.
- `mofacts/client/views/experiment/svelte/services/ttsService.test.ts` now covers platform-prompt TTS runtime resolution for every target UI locale, proving the runtime boundary returns each locale's explicit primary TTS code instead of sampling a subset.
- `mofacts/client/views/experiment/svelte/services/speechRecognitionService.test.ts` now covers UI-locale-derived speech-recognition runtime resolution for every target UI locale when an SR-enabled lesson omits TDF `speechRecognitionLanguage`; explicit TDF speech language still wins.
- A targeted `npm run test -- --runInBand --grep "ttsService|speechRecognitionConfig"` attempt did not start Meteor tests on Windows because the package `test` script uses `TEST_WATCH=1` POSIX env syntax; the previously authorized Meteor integration run was not repeated.
- `npm run typecheck` from `mofacts/` passed after adding all-ten-target-language TTS/SR runtime resolver coverage.
- `npm run lint` from `mofacts/` passed after adding all-ten-target-language TTS/SR runtime resolver coverage.
- `npm run typecheck` from `mofacts/` passed after making pipe-delimited answer alternatives script-agnostic and adding multilingual answer-assessment coverage.
- `npm run lint` from `mofacts/` passed after making pipe-delimited answer alternatives script-agnostic and adding multilingual answer-assessment coverage.
- `npm run typecheck` from `mofacts/` passed after adding all-ten-target-language input/display unit coverage.
- `npm run lint` from `mofacts/` passed after adding all-ten-target-language input/display unit coverage.
- Targeted resource scan confirmed Manual Content Creator speech/TTS/timing labels are no longer literal English in the non-English target-locale blocks for `zh-Hans`, `hi`, `es`, `ar`, `fr`, `bn`, `pt`, `id`, and `ur`.
- `mofacts/client/lib/interfaceI18n.test.ts` now guards the Manual Content Creator audio-label subset against English-copy text in non-English target locales.
- Exact-copy locale-resource audit found remaining English-copy clusters that are mostly product/provider terms, technical labels, or still-unpolished authoring/admin draft strings; Hindi, Bengali, and Urdu Theme editor labels were the largest visible Theme Settings exact-copy cluster and have been partially polished.
- Hindi, Bengali, and Urdu SPARC visible editor/header/toolbar labels have been polished for the high-visibility authoring surface while preserving product/code terms such as `SPARC`, `URL`, `HTML`, `CSS`, `JSON`, and provider names where appropriate.
- Hindi, Bengali, and Urdu core learner prompts for typed-answer guidance, previous-question navigation, and audio replay have been polished to remove visible English fragments such as `Correct answer`, `question`, and `Audio`.
- Hindi, Bengali, and Urdu AutoTutor learner prompts for answer entry and the opening response prompt have been polished to remove visible English fragments such as `answer` and `type`.
- Hindi, Bengali, and Urdu learner runtime performance-stat and learning-progress labels have been polished so visible session timing, correctness, progress-panel, and progress-chart labels are no longer exact English copies.
- Hindi, Bengali, and Urdu AutoTutor runtime labels for progress, expectations, misconceptions, service errors, conversation state, continuation controls, completion messages, and turn counts have been polished while preserving `AutoTutor` as a product term.
- Hindi, Bengali, and Urdu Course/Class Selection labels and alert messages have been polished so enrollment, instructor, course, save, loading, selected, and failure/success messages are no longer exact English copies or mixed-English learner-facing strings.
- Hindi, Bengali, and Urdu Admin Tests labels/status strings have been polished so deployment-readiness headings, descriptions, running/pass/fail messages, table headers, and result statuses are no longer exact English copies.
- Hindi, Bengali, and Urdu Course Assignments labels, controls, loading/empty states, and validation messages have been polished so assignment-editor copy is no longer exact English copies or mixed-English author-facing strings.
- Hindi, Bengali, and Urdu Theme Generation Wizard labels, palette controls, diagnostics labels, preview/status messages, and validation messages have been polished so short visible labels such as light/dark/density/readability/feedback are no longer exact English copies or mixed-English strings.
- Hindi, Bengali, and Urdu Course Management labels, timezone names, section labels, visibility help, save/delete messages, and delete-course warning copy have been polished so the course editor no longer has exact English copies or mixed-English administrative strings.
- Hindi, Bengali, and Urdu AI Content Creator mode/accessibility labels have been polished for source-input ARIA text, character count, session-type labels, learning/assessment mode labels, and chat-tutor short label.
- Hindi, Bengali, and Urdu Content/Stimulus Editor labels, cluster navigation, incorrect-response controls, status messages, media-upload messages, and preview/error labels have been polished while preserving technical literals such as `TDF` and `JSON`.
- Hindi, Bengali, and Urdu TDF Editor labels, condition context, description verbosity controls, validation messages, save/return status, schema-load errors, and editor-library failure copy have been polished while preserving the `TDF` technical term.
- Hindi, Bengali, and Urdu Help page platform chrome has been polished for the student-help heading, administrator-contact prompt, help-content load failure, and online-help-guide link text while preserving authored Markdown help content as content rather than platform UI.
- Hindi, Bengali, and Urdu small visible chrome leftovers have been polished for Debug Menu, Google TTS/SR key labels, Content Manager error label, reporting root-condition labels, and Urdu H5P/admin/reporting/manual-creator shell labels. Literal Manual Creator example placeholders such as `my-experiment` and `biology, unit-1` remain unchanged as example data.
- Hindi, Bengali, and Urdu Canvas IMSCC import wizard chrome, mode controls, quiz/table labels, validation text, generated default instructions, package summary/status copy, upload actions, and package-processing errors have been polished while preserving product/file terms such as `Canvas`, `IMSCC`, `MoFaCTS`, `TDF`, `.imscc`, and `TDF/stim`.
- Hindi, Bengali, and Urdu Anki APKG import wizard chrome, deck-analysis progress, setup labels, note-range/help text, validation messages, generated-file summaries, package/upload status copy, overwrite confirmations, and close-wizard text have been polished while preserving product/file terms such as `Anki`, `.apkg`, `MoFaCTS`, `TDF`, and interpolation placeholders.
- Hindi, Bengali, and Urdu remaining Theme Settings exact-copy labels have been polished for page/library headings, active-theme status, alert/audio/video/surface/performance/loading labels, border-radius labels, use-case help, and transition-speed labels.
- `mofacts/client/lib/interfaceI18n.test.ts` now guards visible Theme editor, Theme Generation Wizard, SPARC editor, core learner-prompt, AutoTutor learner-prompt/runtime, learner performance/progress, Course/Class Selection, Course Management, Admin Tests, Course Assignments, AI Content Creator mode-label, TDF Editor, Content/Stimulus Editor, Help page, small visible chrome, Canvas IMSCC import wizard, and Anki APKG import wizard subsets for Hindi, Bengali, and Urdu against exact English-copy or obvious embedded-English regressions while leaving product/code terms available where appropriate.
- Targeted resource search found no remaining `Correct answer type`, `question पर`, `question এ`, `question پر`, `Audio फिर`, `Audio আবার`, or `Audio دوبارہ` fragments in `mofacts/client/lib/interfaceI18nResources.ts`.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu core learner prompts.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu core learner prompts.
- Targeted locale-resource check confirmed AutoTutor learner prompts have no `answer` or `type` fragments for Hindi, Bengali, or Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu AutoTutor learner prompts.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu AutoTutor learner prompts.
- Targeted locale-resource check confirmed learner performance/progress labels differ from English for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu learner performance/progress labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu learner performance/progress labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu learner performance/progress labels.
- Targeted locale-resource check confirmed all AutoTutor runtime labels differ from English for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu AutoTutor runtime labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu AutoTutor runtime labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu AutoTutor runtime labels.
- Targeted locale-resource check confirmed Course/Class Selection labels differ from English for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Course/Class Selection labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Course/Class Selection labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Course/Class Selection labels.
- Targeted locale-resource check confirmed Admin Tests labels differ from English for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Admin Tests labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Admin Tests labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Admin Tests labels.
- Targeted locale-resource check confirmed Course Assignments labels differ from English and avoid the previous embedded English fragments for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Course Assignments labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Course Assignments labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Course Assignments labels.
- Targeted locale-resource check confirmed the guarded Theme Generation Wizard subset differs from English and avoids the previous embedded English fragments for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Theme Generation Wizard labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Theme Generation Wizard labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Theme Generation Wizard labels.
- Targeted locale-resource check confirmed Course Management labels differ from English and avoid the previous embedded English fragments for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Course Management labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Course Management labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Course Management labels.
- Targeted locale-resource checks confirmed AI Content Creator mode-label locale blocks are assigned to the correct `en`, `hi`, `bn`, and `ur` resources, and the Hindi/Bengali/Urdu labels differ from English without the previous embedded English fragments.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu AI Content Creator mode labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu AI Content Creator mode labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu AI Content Creator mode labels.
- Targeted locale-resource check confirmed the guarded Content/Stimulus Editor subset differs from English and avoids the previous embedded English fragments for Hindi, Bengali, and Urdu; the intentional `contentEditor.json` technical literal remains unchanged outside the guard.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Content/Stimulus Editor labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Content/Stimulus Editor labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Content/Stimulus Editor labels.
- Targeted locale-resource check confirmed TDF Editor labels differ from English and avoid the previous embedded English fragments for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu TDF Editor labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu TDF Editor labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu TDF Editor labels.
- Targeted locale-resource check confirmed Help page chrome differs from English and avoids the previous embedded English fragments for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Help page chrome.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Help page chrome.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Help page chrome.
- Targeted locale-resource check confirmed the small visible chrome prefixes no longer have exact English copies for Hindi, Bengali, or Urdu, except literal Manual Creator slug/tag example placeholders (`my-experiment`, `biology, unit-1`).
- `npm run typecheck` from `mofacts/` passed after polishing the small visible chrome leftovers.
- `npm run lint` from `mofacts/` passed after polishing the small visible chrome leftovers.
- `git diff --check` passed after polishing the small visible chrome leftovers.
- Targeted locale-resource check confirmed Canvas IMSCC import wizard resources differ from English and avoid the previous embedded English fragments for Hindi, Bengali, and Urdu.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Canvas IMSCC import wizard labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Canvas IMSCC import wizard labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Canvas IMSCC import wizard labels.
- Targeted locale-resource check confirmed Anki APKG import wizard resources differ from English and avoid the previous embedded English fragments for Hindi, Bengali, and Urdu, after stripping interpolation placeholders from the scan text.
- `npm run typecheck` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Anki APKG import wizard labels.
- `npm run lint` from `mofacts/` passed after polishing Hindi, Bengali, and Urdu Anki APKG import wizard labels.
- `git diff --check` passed after polishing Hindi, Bengali, and Urdu Anki APKG import wizard labels.
- Targeted locale-resource check confirmed the remaining Theme Settings exact-copy labels differ from English and avoid the previous visible English fragments for Hindi, Bengali, and Urdu; the same check confirmed small wrong-language artifacts in `reporting.rootConditions`, `content.errorLabel`, `apkg.tdfNamePlaceholder`, and `debug.menu` were corrected.
- `npm run typecheck` from `mofacts/` passed after polishing the remaining Hindi, Bengali, and Urdu Theme Settings exact-copy labels.
- `npm run lint` from `mofacts/` passed after polishing the remaining Hindi, Bengali, and Urdu Theme Settings exact-copy labels.
- `git diff --check` passed after polishing the remaining Hindi, Bengali, and Urdu Theme Settings exact-copy labels.
- `npm run typecheck` from `mofacts/` passed after polishing the Hindi, Bengali, and Urdu SPARC visible editor/header/toolbar draft strings.
- `npm run lint` from `mofacts/` passed after polishing the Hindi, Bengali, and Urdu SPARC visible editor/header/toolbar draft strings.
- `npm run typecheck` from `mofacts/` passed after polishing the Hindi, Bengali, and Urdu Theme editor draft strings.
- `npm run lint` from `mofacts/` passed after polishing the Hindi, Bengali, and Urdu Theme editor draft strings.
- `npm run typecheck` from `mofacts/` passed after polishing obvious Manual Content Creator draft-resource leftovers.
- `npm run lint` from `mofacts/` passed after polishing obvious Manual Content Creator draft-resource leftovers.
- `npm run typecheck` from `mofacts/` passed after adding the Manual Content Creator non-English audio-label regression guard.
- `npm run lint` from `mofacts/` passed after adding the Manual Content Creator non-English audio-label regression guard.
- `mofacts/client/views/experiment/svelte/services/ttsService.test.ts` now covers missing platform-prompt voice handling and verifies unsupported UI locale TTS returns `{ status: 'error', textAvailable: true }`.
- `npm run typecheck` from `mofacts/` passed after adding the TTS audio-unavailable result contract.
- `npm run lint` from `mofacts/` passed after adding the TTS audio-unavailable result contract.
- `mofacts/client/lib/manualDraftBuilder.test.ts` now covers Manual Content Creator emission of `contentLanguage`, `recommendedUiLocales`, and `translationStatus` while preserving authored lesson text unchanged.
- `mofacts/client/lib/manualContentCreatorValidation.test.ts` now covers invalid content-language metadata and recommended-UI-locale validation.
- `mofacts/common/lib/interfaceLocales.test.ts` now verifies every target UI locale primary TTS code is present in the provider language-code registry.
- `mofacts/client/lib/manualDraftBuilder.test.ts` now covers Manual Content Creator emission of author-controlled `caseSensitive` and `accentSensitive` matching policy into generated tutor and unit delivery settings.
- `npm run typecheck` from `mofacts/` passed after adding Manual Content Creator language metadata controls.
- `npm run lint` from `mofacts/` passed after adding Manual Content Creator language metadata controls.
- `/contentCreate` sidecar smoke after adding Manual Content Creator language metadata controls redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors.
- Authenticated `/contentCreate` sidecar smoke with the local hotfix admin rendered the Manual Content Creator page and showed the Language metadata panel with Content language, Recommended UI locales, and Translation status controls; console warnings/errors were clean. Save/generate behavior still needs workflow smoke evidence.
- Authenticated sidecar smoke with the local hotfix admin rendered `/profile`, `/home`, `/courses`, `/adminControls`, `/userAdmin`, `/audioSettings`, `/contentUpload`, `/dataDownload`, `/instructorReporting`, `/admin/tests`, `/theme`, `/classEdit`, `/classSelection`, `/tdfAssignmentEdit`, `/aiContentCreate`, `/turkWorkflow`, and `/admin/backups` with no console warnings or errors. These checks covered route rendering only; save, upload, download, readiness-run, backup, Turk, and other mutating actions were not exercised.
- Authenticated `/sparcEdit/placeholder-smoke` sidecar smoke rendered the expected SPARC content-not-found route state with no console warnings or errors; a real SPARC authoring scenario still needs authenticated editor smoke evidence.
- `npm run typecheck` from `mofacts/` passed after adding the primary TTS provider-code registry guard.
- `npm run lint` from `mofacts/` passed after adding the primary TTS provider-code registry guard.
- `npm run typecheck` from `mofacts/` passed after exposing Manual Content Creator typed-answer matching controls.
- `npm run lint` from `mofacts/` passed after exposing Manual Content Creator typed-answer matching controls.
- Authenticated `/contentCreate` sidecar smoke after exposing Manual Content Creator typed-answer matching controls rendered the Manual Content Creator route with no console warnings or errors; step-through save/generate behavior still needs workflow smoke evidence.
- Authenticated `/contentCreate` sidecar workflow smoke created a one-card Spanish-authored lesson with `contentLanguage: es`, recommended UI locales `en, es`, accent-sensitive typed-answer matching, speech recognition unchecked, and text-to-speech set to Neither; it opened the draft editor, finalized to package-ready, saved draft `DKmDrmpkC78q9WBLe`, reloaded that draft id back into the editor, re-finalized, downloaded `Codex-i18n-Spanish-smoke.zip`, uploaded the package into the local hotfix MoFaCTS app, and verified `Codex_i18n_Spanish_smoke` was visible in Content Manager with no console warnings or errors.
- Authenticated mixed-language runtime smoke launched the uploaded Spanish-authored `Codex_i18n_Spanish_smoke` lesson under the English UI, displayed Spanish instruction/prompt text unchanged, accepted the accented typed response `corazón`, and wrote a correct history row with `input: 'corazón'`, `responseValue: 'corazón'`, `CFCorrectAnswer: 'corazón'`, `CFAudioInputEnabled: false`, and `CFAudioOutputEnabled: false`; console warnings/errors were clean. The local smoke TDF timing was extended to 60 seconds after an initial sidecar attempt timed out before input.
- `npm run typecheck` from `mofacts/` passed after preserving the DOM typed-answer value through the Svelte flashcard response chain.
- `npm run lint` from `mofacts/` passed after preserving the DOM typed-answer value through the Svelte flashcard response chain.
- Authenticated `/home` sidecar smoke after scoping dashboard audio feature icons showed the Spanish smoke lesson with only the learning-session feature icon and no TTS/SR icons while its runtime history row retained `CFAudioInputEnabled: false` and `CFAudioOutputEnabled: false`; console warnings/errors were clean.
- `npm run typecheck` from `mofacts/` passed after scoping learner-dashboard audio feature icons to lesson/learner-config metadata.
- `npm run lint` from `mofacts/` passed after scoping learner-dashboard audio feature icons to lesson/learner-config metadata.
- Authenticated mixed-language runtime smoke launched the English-authored `Gen-Z Slang` lesson under the Spanish UI, rendered document `lang="es"` with localized runtime chrome (`Practicar`, `Guardar y volver al menú de práctica`, `PROGRESO`), kept the authored English prompt and answer term unchanged, localized the typed-answer placeholder to `Escribe tu respuesta...`, submitted `wrong answer`, and rendered Spanish platform feedback (`Tu respuesta fue wrong answer.`, `Incorrecto.`, `La respuesta correcta es unc.`); console warnings/errors were clean.
- `npm run typecheck` from `mofacts/` passed after localizing default text-input placeholder, force-correct prompt, and default feedback outcome labels.
- `npm run lint` from `mofacts/` passed after localizing default text-input placeholder, force-correct prompt, and default feedback outcome labels.
- Added `docs-developer/interface-i18n-translation-review-checklist.md` with the translation review/signoff workflow required before non-English AI-draft locales can be marked reviewed or enabled.
- `npm run typecheck` from `mofacts/` passed after localizing learner feedback sentence shells and screen-reader feedback announcements.
- `npm run lint` from `mofacts/` passed after localizing learner feedback sentence shells and screen-reader feedback announcements.
- `/card` sidecar smoke after localizing learner feedback sentence shells redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated feedback rendering still needs lesson-runtime smoke evidence.
- `npm run typecheck` from `mofacts/` passed after threading declared content-language `lang`/`dir` attributes into SPARC text/fraction inputs.
- `npm run lint` from `mofacts/` passed after threading declared content-language `lang`/`dir` attributes into SPARC text/fraction inputs.
- `/sparcEdit/placeholder-smoke` sidecar smoke after threading SPARC input language attributes redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SPARC text-entry rendering still needs scenario smoke evidence.
- Sidecar all-ten-locale smoke on `/auth/login` passed: `en`, `zh-Hans`, `hi`, `es`, `ar`, `fr`, `bn`, `pt`, `id`, and `ur` all updated `html lang`, rendered localized visible login/account prompts, and produced no console warnings or errors; `ar` and `ur` set `html dir="rtl"`.
- `node --experimental-strip-types` resolver check from `mofacts/` passed for platform-prompt TTS primary codes: `en -> en-US`, `zh-Hans -> cmn-CN`, `hi -> hi-IN`, `es -> es-ES`, `ar -> ar-XA`, `fr -> fr-FR`, `bn -> bn-IN`, `pt -> pt-BR`, `id -> id-ID`, `ur -> ur-IN`.
- Hotfix app smoke through the MoFaCTS Playwright sidecar: `/home` redirected to `/auth/login` because the sidecar browser was not signed in; the accessible login route loaded with no console errors.
- Runtime locale smoke through the sidecar: setting `Session.uiLocale` to `ar` and `ur` changed the document to RTL target locale tags and rendered localized platform strings on the login route.
- `/auth/verify-email` sidecar smoke after the email-verification account-flow localization slice loaded directly with no console warnings or errors.
- `/auth/verify-email` sidecar locale smoke after setting `Session.uiLocale` to `ar` produced `lang=\"ar\"`, `dir=\"rtl\"`, and rendered Arabic title/button text.
- `/help` sidecar smoke after the Help page platform-chrome localization slice loaded directly with no console warnings or errors, rendered the `Student Help Guide` heading, and loaded non-empty help Markdown content.
- `/help` sidecar locale smoke after setting `Session.uiLocale` to `ar` produced `lang=\"ar\"`, `dir=\"rtl\"`, and rendered the Arabic help heading.
- `/courses` sidecar smoke redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated course-listing rendering still needs smoke evidence.
- `/home` sidecar smoke redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated learner-dashboard rendering still needs smoke evidence.
- `/home` sidecar smoke after the profile debug-toggle localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated debug-toggle rendering still needs smoke evidence.
- `/home` sidecar smoke after the learner performance-stat localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated lesson-runtime performance rendering still needs smoke evidence.
- `/home` sidecar smoke after the learner learning-progress panel/chart localization cleanup redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated lesson-runtime progress rendering still needs smoke evidence.
- `/home` sidecar smoke after the AutoTutor/SPARC runtime progress localization cleanup redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated AutoTutor/SPARC runtime rendering still needs smoke evidence.
- `/profile` sidecar smoke redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated profile rendering and save behavior still need smoke evidence.
- `/adminControls` sidecar smoke redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated admin rendering still needs smoke evidence.
- `/userAdmin` sidecar smoke after the User Admin usage-cache/API-key metadata display localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated user-admin rendering and actions still need smoke evidence.
- `/userAdmin` sidecar smoke after the User Admin API-key provider-label localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated user-admin rendering and actions still need smoke evidence.
- `/audioSettings` sidecar smoke after the Audio Settings localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated audio settings rendering and control behavior still need smoke evidence.
- `/admin/backups` sidecar smoke after the Admin Backups operation-message localization slice rendered the authenticated Backups page with no console warnings or errors; backup operation behavior still needs explicit action smoke evidence.
- `/contentUpload` sidecar smoke after the Content Manager package-upload/overwrite/processing localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated Content Manager rendering and upload/import workflows still need smoke evidence.
- `/contentUpload` sidecar smoke after the Content Upload error-label localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Content Manager rendering and upload/import workflows still need smoke evidence.
- `/contentUpload` sidecar smoke after the Content Manager condition/stim selector localization cleanup redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Content Manager rendering and upload/import workflows still need smoke evidence.
- `/contentUpload` sidecar smoke after the Anki import wizard upload-step localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated Anki import wizard rendering and workflow behavior still need smoke evidence.
- `/contentUpload` sidecar smoke after the Anki import wizard package/upload-step and validation-error localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Anki import wizard rendering and workflow behavior still need smoke evidence.
- `/dataDownload` sidecar smoke after the Reporting/Data Download localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated data-download rendering and download behavior still need smoke evidence.
- `/instructorReporting` sidecar smoke after the Reporting/Data Download localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated instructor-reporting rendering and exception-action behavior still need smoke evidence.
- `/admin/tests` sidecar smoke after the Admin Tests localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated admin-tests rendering and readiness-run behavior still need smoke evidence.
- `/theme` sidecar smoke after the Theme Generation Wizard localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated theme generation wizard rendering and create/preview behavior still need smoke evidence.
- `/theme` sidecar smoke after the Theme Settings library-chrome localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering and library actions still need smoke evidence.
- `/theme` sidecar smoke after the Theme Settings Custom Help Page localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering and custom-help upload/remove behavior still need smoke evidence.
- `/theme` sidecar smoke after the Theme Settings Branding localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering and branding upload/clear behavior still need smoke evidence.
- `/theme` sidecar smoke after the Theme Settings section-header/default-help localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering still needs smoke evidence.
- `/theme` sidecar smoke after the Theme Settings Main Layout label/help localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering still needs smoke evidence.
- `/theme` sidecar smoke after the Theme Settings Buttons/Interactive label/help localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering still needs smoke evidence.
- `/theme` sidecar smoke after the Theme Settings Menus/Secondary label localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering still needs smoke evidence.
- `/theme` sidecar smoke after the Theme Settings Alerts/Feedback label/help localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering still needs smoke evidence.
- `/theme` sidecar smoke after the Theme Settings Card Surfaces/Overlays label/help localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering still needs smoke evidence.
- `/theme` sidecar smoke after the Theme Settings Border Radius/Animation label localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Theme Settings editor rendering still needs smoke evidence.
- `/classEdit` sidecar smoke after the Course Management localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated course-management rendering and save/delete behavior still need smoke evidence.
- `/classSelection` sidecar smoke after the course/class selection localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated class-selection rendering and save behavior still need smoke evidence.
- `/tdfAssignmentEdit` sidecar smoke after the Course Assignments localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated course-assignment rendering and save behavior still need smoke evidence.
- `/aiContentCreate` sidecar smoke after the AI Content Creator localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated AI Content Creator rendering and creation behavior still need smoke evidence.
- `/contentCreate` sidecar smoke after the Manual Content Creator summary/finalization localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console errors; authenticated Manual Content Creator rendering, draft editing, and upload behavior still need smoke evidence.
- `/contentCreate` sidecar smoke after the Manual Content Creator dynamic content-draft editor localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Manual Content Creator draft editing still needs smoke evidence.
- `/tdfEdit/placeholder-smoke` sidecar smoke after the TDF Editor localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated TDF Editor rendering and save behavior still need smoke evidence.
- `/contentEdit/placeholder-smoke` sidecar smoke after the Content/Stimulus Editor localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Content/Stimulus Editor rendering and save behavior still need smoke evidence.
- `/contentUpload` sidecar smoke after the Canvas IMSCC import wizard upload/analyze-step localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Canvas IMSCC import wizard rendering and workflow behavior still need smoke evidence.
- `/contentUpload` sidecar smoke after the Canvas IMSCC import wizard setup/package/upload localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Canvas IMSCC import wizard rendering and workflow behavior still need smoke evidence.
- `/sparcEdit/placeholder-smoke` sidecar smoke after the SPARC authoring route/header/palette localization slices redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SPARC authoring rendering and save behavior still need smoke evidence.
- `/sparcEdit/placeholder-smoke` sidecar smoke after the SPARC rich-text toolbar and reusable expression-editor localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SPARC authoring rendering and save behavior still need smoke evidence.
- `/sparcEdit/placeholder-smoke` sidecar smoke after the SPARC advanced production-rule editor localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SPARC authoring rendering and save behavior still need smoke evidence.
- `/sparcEdit/placeholder-smoke` sidecar smoke after the SPARC scoped/selected-node side-panel localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SPARC authoring rendering and save behavior still need smoke evidence.
- `/sparcEdit/placeholder-smoke` sidecar smoke after the SPARC visual-surface and rich-text runtime-message localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SPARC authoring rendering and save behavior still need smoke evidence.
- `/sparcEdit/placeholder-smoke` sidecar smoke after the SPARC/learner-runtime leftover label localization cleanup redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SPARC authoring/rendering behavior still needs smoke evidence.
- `/card` sidecar smoke after the runtime accessibility/placeholder label localization cleanup redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated learner-runtime rendering still needs smoke evidence.
- `/sparcEdit/placeholder-smoke` sidecar smoke after the runtime accessibility/placeholder label localization cleanup redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SPARC authoring/rendering behavior still needs smoke evidence.
- `/turkWorkflow` sidecar smoke after the Mechanical Turk admin workflow localization slice redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Turk workflow rendering/action behavior still needs smoke evidence.
- `/turkWorkflow` sidecar smoke after moving Mechanical Turk workflow timestamps to active UI-locale formatting redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated Turk workflow timestamp rendering still needs smoke evidence.
- `/admin/backups`, `/theme`, `/contentUpload`, `/courses`, and `/home` sidecar smokes after moving date formatting and learner-dashboard collation to the active UI locale produced no console warnings or errors; action workflows and lesson-specific timestamp/sort scenarios still need deeper smoke evidence.
- `/card` and `/sparcEdit/placeholder-smoke` sidecar smokes after adding the IME composition guard redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated text-input composition behavior still needs browser/IME smoke evidence.
- `/audioSettings` and `/card` sidecar smokes after requiring explicit speech-recognition language redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated SR warmup/transcription behavior still needs scenario smoke evidence.
- `/profile` sidecar smoke after adding locale review/production-enable status redirected to `/auth/login` in the unauthenticated browser and produced no console warnings or errors; authenticated locale-selector rendering still needs smoke evidence.
- Authenticated `/profile` sidecar workflow smoke changed the local admin's Interface language from `en` to `es`, saved through the visible Save Profile button, rendered document `lang="es"` with Spanish profile chrome and `Perfil guardado`, verified Mongo stored `profile.uiLocale: 'es'`, then restored the same account to `en` through the visible profile form and verified Mongo stored `profile.uiLocale: 'en'`; console warnings/errors were clean.
- Authenticated `/audioSettings` sidecar workflow smoke used the visible Speech Detection Threshold slider to change the local admin's `audioInputSensitivity` from `60` to `55`, verified Mongo stored `audioSettings.audioInputSensitivity: 55`, then restored the slider to `60` through the same UI path and verified Mongo stored the original audio-settings object; console warnings/errors were clean and no microphone prompt path was exercised.
- `npm run typecheck` from `mofacts/` passed after the authenticated Audio Settings workflow smoke.
- `npm run lint` from `mofacts/` passed after the authenticated Audio Settings workflow smoke.
- Shared authenticated app-shell titles now resolve through platform string keys instead of hard-coded English labels; authenticated Spanish-UI sidecar smoke verified `/profile` renders banner `Perfil` and `/audioSettings` renders banner `Configuración de audio` with `html lang="es"` and no console warnings/errors.
- `npm run typecheck` from `mofacts/` passed after localizing shared authenticated app-shell titles.
- `npm run lint` from `mofacts/` passed after localizing shared authenticated app-shell titles.
- Authenticated `/userAdmin` Spanish-UI sidecar workflow smoke filtered the visible user table to `admin@localhost.test`, exercised sortable table headers, verified one visible row, cleared the filter back to the full 23-row list, and produced no console warnings/errors. The smoke also caught and fixed preformatted English range grammar: `admin.showingUsers` now receives localized `start`, `end`, and `total` values separately and rendered `Mostrando 1-1 de 1 usuarios` and `Mostrando 1-23 de 23 usuarios`.
- `npm run typecheck` from `mofacts/` passed after the authenticated User Admin workflow smoke and localized range-grammar fix.
- `npm run lint` from `mofacts/` passed after the authenticated User Admin workflow smoke and localized range-grammar fix.
- Authenticated `/dataDownload` Spanish-UI sidecar workflow smoke rendered `html lang="es"` with localized reporting chrome, downloaded the current admin's own-history TSV as `mofacts_admin@localhost.test_own_history_all_tdfs.tsv`, downloaded the first owned-TDF data file as `TN_Wildflower_Recommended_TDF-data.tsv`, showed the localized success message `Descarga iniciada.`, and produced no console warnings/errors. Authored lesson/file names remained unchanged.
- `npm run typecheck` from `mofacts/` passed after the authenticated Reporting/Data Download workflow smoke.
- `npm run lint` from `mofacts/` passed after the authenticated Reporting/Data Download workflow smoke.
- Authenticated `/admin/tests` Spanish-UI sidecar workflow smoke rendered `html lang="es"` with localized Admin Tests chrome, clicked the visible readiness-check action, and rendered localized status/table labels (`La preparación para despliegue falló`, `Comprobación`, `Estado`, `Mensaje`, `aprobado`, `fallido`). The local hotfix readiness result failed clearly for expected local-environment deployment checks (`settings.source` and `settings.required`) while preserving server diagnostic check names/messages literally; console warnings/errors were clean.
- `npm run typecheck` from `mofacts/` passed after the authenticated Admin Tests readiness workflow smoke.
- `npm run lint` from `mofacts/` passed after the authenticated Admin Tests readiness workflow smoke.
- Authenticated `/courses` Spanish-UI sidecar rendering smoke used a session-only `uiLocale: 'es'` override, rendered `html lang="es"` / `dir="ltr"`, localized course chrome (`Cursos`, `CURSOS ASIGNADOS`, `CURSO`, `TAREA`, `ACCIONES`, `ESTADO`, `ENTREGA`, `INTENTOS`, `PRECISIÓN`, `ELEMENTOS`, `DÍAS`, `TIEMPO`, `ÚLTIMA PRÁCTICA`, and `No hay cursos públicos disponibles.`), preserved authored course/teacher names, and produced no console warnings/errors.
- Authenticated `/home` Spanish-UI sidecar rendering smoke used a session-only `uiLocale: 'es'` override, rendered `html lang="es"` / `dir="ltr"`, localized learner-dashboard chrome (`Practicar`, `Continuar`, metric labels, table headers, and dashboard actions), preserved authored lesson names, and produced no console warnings/errors. The visible `Welcome back!`/`Access your practice...` copy came from the active custom theme `imperial-dark-488a` (`Whimsical Refined`) as admin-authored `practice_menu_welcome_html`, so it was intentionally preserved rather than translated as platform chrome.

## Remaining Surfaces

- Authenticated action/workflow smokes remain for backup operations, direct Content Manager upload/import actions, Theme preview/create/library/customization actions, Course Management save/delete, Course Assignments save, AI Content Creator creation, SPARC authoring save behavior, Mechanical Turk actions, and lesson-specific learner runtime fallback controls for video/H5P/display-timeout scenarios.
- Broad visible HTML/Svelte sweep currently reports only product/data abbreviations or examples (`TDF`, `HTML`, `HL`, `AutoTutor`, font-family example text) plus `termsOfService.html`. The Terms of Service page is authored legal/public-document text and should not be treated as ordinary interface chrome without explicit legal/review policy.
- Qualified reviewer signoff for non-English AI-draft strings.
- Remaining mixed-language lesson scenario smoke evidence for broader script coverage beyond the English-UI/Spanish-content typed-answer path and Spanish-UI/English-content runtime path.
- Provider-backed platform-prompt TTS playback smoke evidence for all ten primary TTS codes.
