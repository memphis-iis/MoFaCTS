# Migration Scripts

Target home for one-time or repeatable migration scripts.

Migration scripts should fail clearly when prerequisites are missing and should not silently fall back to old structures.
