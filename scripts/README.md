# Scripts

Target home for root-level development, audit, migration, and release scripts.

Current status: scaffold. Existing executable app scripts remain under `mofacts/scripts/`.

Do not move scripts here until command paths in docs, CI, package scripts, and deploy workflows are updated together.
