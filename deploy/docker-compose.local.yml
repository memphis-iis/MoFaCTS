services:
  # The canonical localhost app runs natively through hotfix-local.ps1 so
  # Meteor and Rspack can watch the source tree. Docker owns only its services.
  mongodb:
    ports:
      - "127.0.0.1:27017:27017"
