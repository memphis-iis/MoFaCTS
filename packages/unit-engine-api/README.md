# Unit Engine API

Target package for the stable unit engine public contract.

Current status: architectural scaffold. The first concrete interface should be extracted from the existing `unitEngine.ts` flow without changing behavior.
