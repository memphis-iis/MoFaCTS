# Model Policy API

Target package for adaptive model policy contracts.

Current status: architectural scaffold. This should expose stable policy interfaces, not implementation-specific model internals.
