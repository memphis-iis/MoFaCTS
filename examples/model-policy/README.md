# Model Policy

Copyable template for an adaptive selection policy.

Current status: typed template covered by the repository typecheck through the migration bridge.

Start with `ClosestProbabilityPolicy.ts`. It shows a visible, fail-clear adaptive selection policy over explicit probability candidates.

Use this when a contributor wants to choose the next card from explicit model state.
