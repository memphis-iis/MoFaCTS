# Minimal Trial Type

Copyable template for a new trial interaction.

Current status: typed template covered by the repository typecheck through the migration bridge.

Start with `MinimalTextTrial.ts`. It shows the smallest useful trial boundary: receive a prompt and learner response, return an explicit trial result.

Use this when a contributor wants to render a learner interaction and report the result back to a unit engine.
